/** Define os possíveis estados de uma sessão de exibição. */
export enum ScreeningAdministrativeStatusEnum {
    /** Agendada normalmente. */
    SCHEDULED = 'SCHEDULED',

    /** Cancelada (não ocorrerá). */
    CANCELLED = 'CANCELLED',
}