/**
 * Enum que representa os idiomas suportados pela aplicação.
 * Cada membro corresponde a um código de idioma padrão (ISO 639-1).
 */
export enum SupportedLanguageEnum {
  PT = 'PT',
  EN = 'EN',
}
